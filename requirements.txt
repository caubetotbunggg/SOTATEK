opencv-python-headless>=4.5.0,<5.0.0
pillow>=8.0.0,<11.0.0
numpy>=1.21.0,<2.0.0
scipy>=1.7.0,<2.0.0
scikit-image>=0.18.0,<1.0.0
gradio==4.44.1
# Keep gradio_client in sync with Gradio 4.44.x (avoids JSON schema parsing errors in `get_api_info()`).
gradio_client==1.3.0
# Gradio 4.x imports `HfFolder` from huggingface_hub, which was removed in v1.x.
huggingface_hub<1.0.0
audioop-lts>=0.2.1; python_version >= "3.13"
